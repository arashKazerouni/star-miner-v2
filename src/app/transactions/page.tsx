"use client"; import {useEffect,useState} from "react"; import Link from "next/link"; export default function Page(){const [items,setItems]=useState<any[]>([]);async function load(){const r=await fetch("/api/payments");if(r.ok)setItems(await r.json())}useEffect(()=>{load()},[]);async function verify(id:string){await fetch(`/api/payments/${id}/verify`,{method:"POST"});load()}return <main className="dash"><nav><Link href="/dashboard" className="brand">✦ STELLAR MINER</Link><Link href="/boost">Boost Store</Link></nav><p className="eyebrow">PAYMENT HISTORY</p><h1>Your transactions</h1><div className="history">{items.map(p=><article key={p.id}><b>{p.boostPackage.name}</b><span>{(Number(p.expectedStroops)/1e7).toFixed(2)} XLM</span><span className={`status ${p.status.toLowerCase()}`}>{p.status}</span><small>Memo: {p.memo}</small>{p.status==="PENDING"&&<button onClick={()=>verify(p.id)}>Check payment</button>}{p.stellarTransactionHash&&<small>Hash: {p.stellarTransactionHash}</small>}</article>)}{!items.length&&<p>No payments yet. <Link href="/boost">Explore boosts.</Link></p>}</div></main>}
